# GENERATED BY gwctl apply — DO NOT EDIT BY HAND.
#
# Source of truth: config/proxy.yaml and the target order in config/models.yaml.
# Model deployments and virtual keys are NOT here: they live in Postgres and are
# reconciled through the LiteLLM Admin API (store_model_in_db).
#
# Changes to this file take effect only after the proxy is restarted:
#   make restart
model_list: []
general_settings:
  allow_requests_on_db_unavailable: false
  database_url: os.environ/DATABASE_URL
  disable_spend_logs: false
  master_key: os.environ/LITELLM_MASTER_KEY
  store_model_in_db: true
  store_prompts_in_spend_logs: false
litellm_settings:
  cache: false
  callbacks:
    - gw_hooks.gw_handler
  drop_params: true
  num_retries: 2
  request_timeout: 30
  turn_off_message_logging: true
router_settings:
  allowed_fails: 3
  cooldown_time: 30
  enable_pre_call_checks: true
  fallbacks:
    - balanced:
        - balanced--fallback-1
    - cheap-fast:
        - cheap-fast--fallback-1
    - long-context:
        - long-context--fallback-1
    - smart:
        - smart--fallback-1
    - vision:
        - vision--fallback-1
  num_retries: 2
  redis_host: os.environ/REDIS_HOST
  redis_port: os.environ/REDIS_PORT
  retry_after: 1
  routing_strategy: simple-shuffle
  timeout: 30
